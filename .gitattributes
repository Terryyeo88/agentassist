*.pdf binary
*.xlsx binary

# T2.12a ground-truth fixtures: pin to LF so sha256 manifest hashes are
# byte-stable on every platform (Windows capture must not introduce CRLF).
tests/fixtures/sbodemosg-extract/*.json text eol=lf

# T2.12a synthetic extract-export fixtures: pin to LF so the byte-stability
# round-trip holds across Windows capture / Linux CI.
tests/fixtures/extract-export-sbodemosg/*.csv text eol=lf
